spring:
  application:
    name: shopping-store
  datasource:
    url: jdbc:postgresql://localhost:5432/analyzer
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        default_schema: shopping_store
        format_sql: true
  sql:
    init:
      mode: always  # ← ИЗМЕНИТЬ
      schema-locations: classpath:schema.sql  # ← ДОБАВИТЬ

server:
  port: 0

eureka:
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname:localhost}:${eureka.instance.port:8761}/eureka/

logging:
  level:
    ru.yandex.practicum: INFO